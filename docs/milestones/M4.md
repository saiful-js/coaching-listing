# M4 — Images (Cloudinary): completion note

- **Date:** 2026-09-22
- **Status:** Done (DoD met, direct on `main`)
- **Plan:** `docs/milestones/M4-plan.md` · **Arch:** ADR 0004 · **Ledger:** `.superpowers/sdd/plan/progress.md`

## What was built

| Item | Where |
|---|---|
| Env + gate | `CLOUDINARY_CLOUD_NAME/API_KEY/API_SECRET` optional; prod boot requires all three; user credentials configured locally + ping-verified |
| Client seam | `features/uploads/cloudinary-client.ts` — real SDK via `upload_stream`, `destroy` with invalidate, `cloudinaryUrl` (`w_1600,c_limit,f_auto,q_auto`) |
| Service | `features/uploads/service.ts` — type/size(bytes)/count validation, 20-uploads/hour owner limit, auto-cover first, single cover (+ DB partial unique backstop), delete-with-best-effort-destroy + cover promotion, setCover |
| Actions | `features/uploads/actions.ts` — thin wrappers, Zod id validation, default-deny error allowlist |
| UI | `ImageUploader`, `ImageGallery` + `CoverThumb`, `ImageActions` on the edit page; cover thumbs on dashboard; `remotePatterns` locked to `res.cloudinary.com`; Server Action body cap raised to 6 MB (5 MB files + multipart overhead) |
| Submit rule | `submitForReview` requires a cover (FR-3); M3 tests updated |

## DoD evidence

- `biome` ✓ · `tsc` ✓ · `vitest` 85/85 ✓ · `next build` ✓
- Abuse cases tested: bad type, oversize (claimed + actual bytes), 7th image, cross-owner 404, 21st upload → 429, duplicate-key cleanup, double-cover index backstop.
- **Live proof:** one real upload + HEAD 200 on the delivery URL + delete against the owner's cloud (throwaway test, removed afterwards).
- Fresh review: no Criticals; 7 Important fixed in one pass (see ledger).

## Deviations / decisions

1. Server-side upload via Actions (no presign/CORS/compression lib); `browser-image-compression` gate dropped.
2. `key` = Cloudinary `public_id`; failed DB writes best-effort destroy the orphan.
3. Rate-limit block verified by temporary removal (RED) + restore (GREEN); the restore briefly reverted the whole file via `git checkout` — re-applied edit-by-edit and re-verified.

## Deferred minors (review)

MN-01 pending stuck on set-cover success; MN-02 `sortOrder` gaps after delete; MN-03 delete+promote non-atomic (fail-safe: submit blocks coverless); MN-04 raw `publicId`/width in delivery URL; MN-05 magic `6` in uploader + UI-dead `asCover` param; MN-06 gallery imports via server barrel (stays server-side); MN-07 generic gallery alt text (M7 owns a11y).

## Next (M5 — Public pages)

`/` (search, area chips, latest), `/coachings` (filters in URL, 12/page, `publishedAt desc, id desc`), `/coachings/[slug]`, `/areas/[slug]`; Server Components + card-field selects + cover; empty/error/loading states; Bengali font for user content; `tel:`/`wa.me` buttons. Perf targets at DoD.
