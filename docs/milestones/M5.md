# M5 — Public pages: completion note

- **Date:** 2026-09-22
- **Status:** Done (DoD met, direct on `main`)
- **Plan:** `docs/milestones/M5-plan.md` · **Ledger:** `.superpowers/sdd/plan/progress.md`

## What was built

| Item | Where |
|---|---|
| Queries | `features/listings/queries.ts` — PUBLISHED-only reads, 12/page, `publishedAt desc, id desc`, insensitive contains search, area/category filters, card-minimal selects |
| Directory | `/coachings` — GET filter form (q/area/category, shareable URLs), pagination, empty + clear-filters, `loading.tsx`, `error.tsx` |
| Detail | `/coachings/[slug]` — gallery, `whitespace-pre-line` details, `tel:`/`wa.me`/`mailto`/Facebook contact card, `not-found.tsx`, per-page metadata |
| Home | `/` — search box, DB area chips with live counts, latest 6, owners section; static with 5-min revalidate |
| Areas | `/areas/[slug]` — SEO landings with unique titles + published lists |
| Type | Hind Siliguri content face (`font-content`) for user-entered Bangla/English text |
| Card | `ListingCard` (server) — cover via `cloudinaryUrl` + `next/image` with sizes, no-cover initial fallback |

## DoD evidence

- Only PUBLISHED publicly visible: 7 integration tests (visibility across all 5 statuses, ordering, pagination, search, filters, detail-null, area/latest, page normalization).
- `biome` ✓ · `tsc` ✓ · `vitest` 94/94 ✓ · `next build` ✓ (public routes dynamic except home ○).
- Fresh review: no Criticals; 2 Important fixed (render-time Facebook href guard with unit tests; out-of-range page clamp).

## Deviations / decisions

1. No `nuqs` — native GET forms + `searchParams` give the same shareable-URL guarantee with zero client JS.
2. Plain title/description metadata now; canonical/OG/JSON-LD stay M7.
3. Perf targets (LCP/CLS/INP on a device profile) unmeasurable here — shipped static + sized images + zero page-level JS; real measurement at M7 PageSpeed.

## Deferred minors (review)

M-01 "zero JS" wording (header island is client); M-02 `mailto:` encoding; M-03 double area fetch in `areasWithCounts`; M-04 unencoded slug interpolation; M-05 no root `not-found.tsx`; M-06 unbounded `q` length.

## Next (M6 — Admin moderation)

`/admin/listings?status=PENDING` queue (approve/reject-with-reason/unpublish via `service.ts` only), minimal area/category management, E2E: only PUBLISHED publicly visible + audit row per transition.
