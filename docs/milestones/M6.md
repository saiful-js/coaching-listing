# M6 — Admin moderation: completion note

- **Date:** 2026-09-22
- **Status:** Done (DoD met, direct on `main`)
- **Plan:** `docs/milestones/M6-plan.md` · **Ledger:** `.superpowers/sdd/plan/progress.md`

## What was built

| Item | Where |
|---|---|
| Guard | `app/admin/layout.tsx` — `requireAdmin()` (401 → login, else → home) |
| Queue | `app/admin/listings` — status tabs incl. Drafts + All, 25/page pagination with counts, owner email + area per row, `ModerationActions` (approve / reject-with-reason / unpublish) |
| Taxonomy | `app/admin/taxonomy` — areas + categories with listing counts, create forms (unique slugs, fail-closed) |
| Actions | `features/admin/actions.ts` — thin wrappers, Zod ids + reason (1–2000 chars), Prisma errors stay server-side |
| Service | `features/admin/service.ts` — admin-only `createArea`/`createCategory`, Bangla-only names rejected (no `item` squat) |
| Nav | Conditional Admin link for `role === "ADMIN"` sessions (fail-closed) |

## DoD evidence

- Only PUBLISHED publicly visible: approve→visible, reject/unpublish→hidden (integration-tested via `listPublished` + `getPublishedBySlug`).
- Every transition writes an audit row: full `DRAFT→PENDING→PUBLISHED→ARCHIVED` chain asserted.
- Non-admin approve/reject → 403; cross-owner archive → 404.
- `biome` ✓ · `tsc` ✓ · `vitest` 98/98 ✓ · `next build` ✓ (`/admin/*` dynamic).
- Fresh review: no Criticals; 3 Important fixed (queue pagination, sluggable guard, reason validation).

## Deviations / decisions

1. Taxonomy rename/delete out (FK risk); create-only.
2. No rate limits on admin-only actions (single seeded admin, A8).
3. Admin UI surfaces service messages (Prisma-coded errors mapped generic).

## Deferred minors (review)

M-1 home ISR staleness after moderation (≤300s); M-2 P2002 slug race message; M-3 transport-failure button state; M-4 Prisma queries in admin pages (layout guard holds); M-5 layout catch-all redirect; M-6 DRAFT tab (done as part of I-1).

## Next (M7 — SEO + a11y)

Metadata + canonicals, `sitemap.xml`, `robots.txt`, JSON-LD, OG images, semantic HTML, full a11y pass, PageSpeed measurement. Then M8 hardening + launch.
