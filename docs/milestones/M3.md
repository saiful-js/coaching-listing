# M3 — Owner listings (no images): completion note

- **Date:** 2026-09-22
- **Status:** Done (DoD met, direct on `main` per user preference)
- **Plan:** `docs/milestones/M3-plan.md` · **Ledger:** `.superpowers/sdd/plan/progress.md`

## What was built

| Item | Where |
|---|---|
| Schemas (server truth) | `features/listings/schemas.ts` — name 3–120, area, address 5–250, details 30–5000, 1–5 categories, BD-mobile phone (+880 normalized), optional WhatsApp/email/Facebook (https + fb hosts) |
| Slug | `features/listings/slug.ts` — `slugify(name) \|\| "coaching"` + 6 chars, once, immutable (tested) |
| Policies | `features/listings/policies.ts` — pure transition matrix + canEdit/canSubmit/canArchive/canModerate (19 unit tests) |
| Service (only status writer) | `features/listings/service.ts` — create/update/submit/approve/reject/archive, atomic transition+audit via `$transaction`, verified-only submit, 5-active cap, create rate limit (10/h via `consumeRateLimit`), 404-not-403, admin-submit forbidden |
| Actions | `features/listings/actions.ts` — thin Actor-resolving wrappers returning `{ok, id, slug, error}` |
| UI | `/dashboard` (badges + reviewer notes + empty state), `/dashboard/listings/new`, `/dashboard/listings/[id]/edit` (404-not-403), `ListingForm`, `DashboardActions`, `StatusBadge` (clay/forest tokens) |
| Boundary | `features/listings/index.ts` is server-only (documented); client components import `./actions` directly (build caught a `pg`-in-browser leak — fixed) |

## DoD evidence

- Owner cannot touch others' listings: cross-owner update/submit → `NotFoundError` (integration-tested); edit page `notFound()` (same look as missing).
- `biome` ✓ · `tsc` ✓ · `vitest` 72/72 ✓ · `next build` ✓ (routes: `/dashboard`, `new`, `[id]/edit` dynamic).
- Fresh review: no Criticals; 1 Important fixed (admin-submit overreach, RED→GREEN); minors deferred below.

## Deviations / decisions

1. No `react-hook-form` (FormData+Zod continues); no new deps at all.
2. Unpublish → ARCHIVED single target (PRD's REJECTED alternative is an M6 call).
3. Cover-required-to-submit deferred to M4 (no storage yet).
4. Creation writes no audit row (not a transition); `publishedAt` set only on →PUBLISHED.

## Deferred minors (review)

M-1 transition read outside tx; M-2 P2002 slug race → friendly error; M-3 `toError` raw DB text → generic+log; M-4 dashboard pending stuck on success; M-5 `facebookUrl: ""` vs NULL + dup-category message; M-6 `id` param validation (fail-closed today).

## Next (M4 — Images, R2)

Approval gate stands: provision R2 bucket + image domain first (user chose defer/stub), then presign route, client compression (`browser-image-compression` needs approval), `HeadObject` verification on save, `remotePatterns`, abuse-case tests. Submit gains the cover-required check.
