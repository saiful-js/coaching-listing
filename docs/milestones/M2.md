# M2 — Auth: completion note

- **Date:** 2026-09-22
- **Status:** Done (DoD met; branch `feat/m2-auth`, worktree `.worktrees/m2-auth`)
- **Plan:** `docs/milestones/M2-plan.md` · **Ledger:** `.superpowers/sdd/plan/progress.md`

## What was built

| Item | Where |
|---|---|
| Auth config | `src/lib/auth.ts` — email/password (min 8), verification + reset mail via stub outbox, Google gated on both keys, `captcha` plugin gated on secret, persisted `rateLimit` (sign-in 5/600s, sign-up 10/600s, reset 5/600s, `enabled: true` explicit), `baseURL` from `APP_URL` |
| Stub mail | `src/lib/email.ts` — outbox only when keyless; Resend HTTP delivery (no new dep) when keyed, retains nothing |
| Turnstile stub | `src/lib/turnstile.ts` — open outside prod, closed in prod; prod boot additionally refused without the secret |
| Env gates | `src/lib/env.ts` — M2 keys optional in dev/test; production refuses to boot without 32+ secret, https non-localhost `APP_URL`, Resend + Turnstile keys |
| Rate limiter (app) | `src/lib/rate-limit.ts` + `RateLimitHit` model/migration — for M3/M4 non-auth mutations (auth endpoints use the built-in limiter) |
| Guards | `src/lib/auth-helpers.ts` — `requireUser` (401), `requireAdmin` (403), `requireOwnerOf` (404-not-403, admin bypass); carries `emailVerified` for the M3 submission gate |
| API route | `src/app/api/auth/[...all]/route.ts` (`toNextJsHandler`) |
| Pages/forms | `(auth)/login\|register\|forgot-password\|reset-password\|verify-email` + `src/features/auth/*` (shared Zod, thin client forms, `verified=1` success branch, captcha-token plumbing) |
| Tests | 7 files, 34 tests: unit (env, rate-limit, email, turnstile, schemas, guards) + `tests/integration/auth-journey.test.ts` (register→verify→login→logout, 6×wrong→429, reset single-use+expiring) |

## DoD evidence

- `biome check` ✓ · `tsc --noEmit` ✓ · `vitest run` 34/34 ✓ · `next build` ✓ (with dummy prod-shaped env; keyless build correctly refuses at page collection)
- Fresh whole-branch review done; Critical/Important fixed in one pass (see ledger), minors deferred below.

## Deviations / decisions

1. **Dev stubs, no new deps** (user-approved): no `resend`, no Turnstile widget package, no Playwright — journey proven via integration tests against the real handler + DB.
2. **Two limiters, split by domain**: built-in persisted limiter for auth endpoints; hand-rolled `consumeRateLimit` for M3/M4 mutations.
3. **Logout UI deferred to M3** dashboard (no `/logout` route in PRD); logout API proven in the journey test.
4. **Cookies on Better Auth production-mode defaults** (httpOnly+secure per docs); browser-level proof deferred to M8.

## Deferred (from review)

- Minors: M-1 zod email/normalize order; M-2 pending reset on nav failure; M-3 Resend error embeds recipient; M-4 `coachingId` Zod at boundary (M3 owns); M-6 `noreply@example.com` sender = provisioning debt; M-7 `noValidate={false}` no-op.
- M8 security review: rate-limit IP keying/spoofing + `trustedProxies`; cookie attributes in a real browser; OAuth initiation smoke test.

## Provisioning debt (needs real accounts)

- Resend key + sender domain; Turnstile secret + site key + widget (checklist in `src/lib/auth.ts` — secret without widget breaks forms); Google OAuth client (undecided at launch per plan gates).

## Next (M3 — Owner listings, no images)

Shared Zod listing schema, `service.ts` (only status writer) + `policies.ts`, dashboard CRUD, immutable slug, audit rows, 404-not-403 + transition-matrix tests. Unverified-submit gate uses `SessionUser.emailVerified` (ready).
